package 데브매칭코테;

public class sql {
/*
 -- 코드를 입력하세요
SELECT
    DISTINCT(C.USER_ID)
FROM
    CARTS AS C 
    LEFT JOIN
    CART_PRODUCTS AS P
    ON
    C.ID = P.CART_ID
WHERE
    P.NAME LIKE '밀가루' 
 */
}
